pipeline {
  agent any

  parameters {
    string(name: 'CONFIRM', defaultValue: '', description: 'Tapez "OUI" pour confirmer la destruction.')
  }

  stages {
    stage('📥 Copier terraform.tfstate du job apply') {
      steps {
        copyArtifacts(
          projectName: 'nom_du_job_apply', // ← à adapter
          selector: lastSuccessful(),
          filter: 'terraform.tfstate',
          target: '.',
          flatten: true
        )
      }
    }

    stage('🧹 Confirmation') {
      when {
        expression { return params.CONFIRM == 'OUI' }
      }
      steps {
        echo "✅ Confirmation validée. On lance la destruction."
      }
    }

    stage('🔍 Vérification tfstate') {
      when {
        expression { return params.CONFIRM == 'OUI' }
      }
      steps {
        sh '''
          echo "Contenu du répertoire courant :"
          ls -lh
          echo "Contenu de terraform.tfstate :"
          cat terraform.tfstate || echo "❌ Fichier manquant"
        '''
      }
    }

    stage('⚠️ Terraform Destroy') {
      when {
        expression { return params.CONFIRM == 'OUI' }
      }
      steps {
        withCredentials([
          string(credentialsId: 'AWS_ACCESS_KEY_ID', variable: 'AWS_ACCESS_KEY_ID'),
          string(credentialsId: 'AWS_SECRET_ACCESS_KEY', variable: 'AWS_SECRET_ACCESS_KEY')
        ]) {
          sh '''
            terraform init
            terraform destroy -auto-approve
          '''
        }
      }
    }

    stage('⛔ Annulation') {
      when {
        expression { return params.CONFIRM != 'OUI' }
      }
      steps {
        echo "❌ Destruction annulée. Entrez CONFIRM=OUI pour valider."
      }
    }
  }

  post {
    always {
      echo "🧾 Fin du pipeline de destruction."
    }
  }
}
